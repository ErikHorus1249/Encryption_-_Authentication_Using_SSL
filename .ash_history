ls
ls -l
keytool -keystore kafka.server.keystore.jks -alias localhost -validity 10 -genkey
openssl req -new -x509 -keyout ca-key -out ca-cert -days 365
apt install openssl
cd 
ls
cd ..
ls
nano kafka.server.keystore.jks 
vi kafka.server.keystore.jks 
ls
cls
clear
apt-get install nano
sudo apt-get update && sudo apt-get upgrade
apt-get update && sudo apt-get upgrade
openssl req -new -x509 -keyout ca-key -out ca-cert -days 365
clear
ls
exit
nano kafka
cls
clear
ls
vi kafka.server.keystore.jks 
history
pwd
openssl req -new -x509 -keyout ca-key -out ca-cert -days 365
cls
lsb_release -a
cls
clear
ls
cd etc/
ls
nano issue 
vi issue 
ls
cd apk/
ls
vi repositories 
cd
apk add openssl
cls
clear
openssl req -new -x509 -keyout ca-key -out ca-cert -days 365
ls
apk add vim
vim ca-cert 
keytool -keystore kafka.server.truststore.jks -alias CARoot -import -file ca-cert
keytool -keystore kafka.client.truststore.jks -alias CARoot -import -file ca-cert
keytool -keystore kafka.server.keystore.jks -alias localhost -certreq -file cert-file
openssl x509 -req -CA ca-cert -CAkey ca-key -in cert-file -out cert-signed -days 10 -CAcreateserial -passin pass:1421999@Tuananh
cd ..
ls
cp kafka.server.keystore.jks ~/
cd 
ls
keytool -keystore kafka.server.keystore.jks -alias localhost -certreq -file cert-file
keytool -keystore kafka.server.keystore.jks -alias localhost -validity 10 -genkey
cd ..
ls
rm kafka.server.keystore.jks 
ls
cls
clear
cd 
ls
rm kafka.client.truststore.jks 
ls
rm kafka.server.keystore.jks 
rm kafka.server.truststore.jks 
ls
cls
clear
keytool -keystore kafka.server.keystore.jks -alias localhost -validity 10 -genkey
cls
vi /bin/sh 
clear
ls
openssl req -new -x509 -keyout ca-key -out ca-cert -days 365
ls
clear
ls
keytool -keystore kafka.server.truststore.jks -alias CARoot -import -file ca-cert
keytool -keystore kafka.client.truststore.jks -alias CARoot -import -file ca-cert
ls
keytool -keystore kafka.server.keystore.jks -alias localhost -certreq -file cert-file
openssl x509 -req -CA ca-cert -CAkey ca-key -in cert-file -out cert-signed -days 10 -CAcreateserial -passin pass:1421999@Tuananh
keytool -keystore kafka.server.keystore.jks -alias CARoot -import -file ca-cert
keytool -keystore kafka.server.keystore.jks -alias localhost -import -file cert-signed
clear
ls
vim kafka.server.keystore.jks 
clear
apk add git
clear
ls
vim README.d
git init
git add .
